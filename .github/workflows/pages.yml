name: Build and deploy

on:
  push:
    branches: [main]
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: pages
  cancel-in-progress: false

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'

      - name: Install dependencies
        run: |
          sudo apt-get update -qq
          sudo apt-get install -y --no-install-recommends xsltproc libxml2-utils
          python -m pip install --quiet lxml

      - name: Check the TEI file
        run: |
          xmllint --noout ten-great-novels.xml
          python3 consistency.py ten-great-novels.xml

      - name: Build the site and the data files
        run: sh build.sh

      - name: Collect the files to publish
        run: |
          mkdir -p _site
          cp index.html query.rq correspondents.geojson \
             ten-great-novels.graphml ten-great-novels.xml _site/

      - uses: actions/upload-pages-artifact@v3
        with:
          path: _site

  deploy:
    needs: build
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    steps:
      - id: deployment
        uses: actions/deploy-pages@v4
